{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // we are connecting this to the ssms and we will add the name of the ssms
  "ConnectionStrings": {
    "SqlServerConnStr": "server=V\\SQLEXPRESS; database=Authentication_db; Integrated Security=true; Encrypt=False"
  }
}
